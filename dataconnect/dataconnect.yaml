specVersion: "v1"
serviceId: "fbp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fbp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
